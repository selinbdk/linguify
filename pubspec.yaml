name: linguify
description: A new Flutter project.
publish_to: none
version: 1.0.0+1

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  cupertino_icons: ^1.0.2
  dio: ^5.5.0+1
  dropdown_button2: ^2.3.9
  easy_debounce: ^2.0.3
  flutter:
    sdk: flutter
  flutter_dotenv: ^5.1.0
  json_annotation: ^4.9.0
  provider: ^6.1.2

dev_dependencies:
  build_runner: ^2.4.11
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter
  json_serializable: ^6.8.0
  very_good_analysis: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - .env
    - list_of_languages.json
